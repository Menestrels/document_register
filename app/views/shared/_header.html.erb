<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <%= link_to root_path, class: "navbar-item" do %>
            <span class="has-textweight-bold is-size-4 ml-4">Document Register</span>
        <% end %>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
            <% if user_signed_in? %>
                <%= link_to "Edit account", edit_user_registration_path, class: "button is-link" %>
                <%= link_to "Log out", destroy_user_session_path, method: :delete, class:"button is-light" %>
            <% else %>
                <%= link_to "Login", new_user_session_path, class:"button is-link" %>
                <%= link_to "Sign Up", new_user_registration_path, class:"button is-light" %>   
            <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>